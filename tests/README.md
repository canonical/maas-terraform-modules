## Tests

These tests run to validate the example stacks in `examples/stacks/`. Smoke tests are run on all stacks on every PR and push to main, and full tests as run on a nightly basis. Please refer to the relevant GitHub Actions workflow files in `.github/workflows/` for more details on the testing process.
